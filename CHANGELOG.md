# 更新日志

本项目的所有值得注意的改动都会记录在这个文件里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 修复

- **切板块之后右侧图形大半是空的**（真 bug）。配对原本是"每颗粒子找离自己最近的格子"，结果是**后一个图形大半格子分不到粒子** —— 点都堆在两个图形重叠的那块区域去抢同一个最近点；实测最惨的一次：3267 个格子只分到 539 颗粒子，切过去就是一片空白。现在改成两边先按**空间填充曲线（Morton 序）**排序、再一一对应：完整性和"就近"同时拿到（每个格子必定有粒子，位移又被限制在邻近区域）。中间试过"两边各自按 上→下 / 左→右 排序后对应" —— 图形是完整了，但能配出很长的位移（人像头顶的点被配到时间轴左下角），过渡中间糊成一团雾，已弃用。

- **PC 上粒子归位之后会顿一下**。归位结束那一帧里同时做了四件重活：摘掉 `hero--intro`（层级变化）、改完样式后立刻读一次布局（强制同步布局）、把画布位图从 2880×1800 重新分配成 3024×688（约 8MB）再重画 1.5 万颗点；页眉的 z-index 也正好在这一刻从 9999 掉回 900。逐帧测下来这一帧的 JS 是平时的 ~4 倍，而单独测那几件操作又都是 0.00ms —— 说明成本不在 JS，而在这之后的一次性合成 / 光栅化，PC 上表现出来就是掉一帧。现在：
  - 落定那一帧**只改状态 + 收尾绘制**，画布交接（摘 `hero--intro`、量布局、换尺寸）延后 `HANDOVER_MS`(120ms)、等画面彻底静止再做；内容一模一样，只是换块画布，看不出来；
  - 页眉的 z-index 不再回落（它本来就该在内容之上），少一次层级变化；
  - 交接期间主循环不会停（`settled` 里加了 `!handoverAt`），做完才允许停下。
- **停在页面中间刷新时，加载界面上一个粒子都没有**。浏览器会恢复上次的滚动位置，而"滚动散开"那条进度会把所有点的透明度乘成 0 —— 于是铺满视口的开场云变成一块空屏。现在两处一起管住：
  - `index.html` 里加了一小段脚本，**刷新（navigation type = reload）一律从顶部开始**，并把 `history.scrollRestoration` 置为 `manual`；前进 / 后退仍然保留浏览器的位置恢复，带 `#锚点` 打开也照常跳到锚点；
  - `ParticleMountain` 里再加一道兜底：开场（无序云 + 归位）期间滚动进度强制为 0，就算页面真的停在中间，点也必须一颗不少地看得见。
- **归位途中的排版回流会让整段过渡凭空消失**（真 bug，第一版没抓到）。网页字体加载完替换字体、或滚动锁解除时滚动条出现，都会让首屏回流几十像素；归位的 1.5 秒里只要撞上一次，`ResizeObserver` 就会重建整批粒子 —— 点集合变了（实测 8286 → 6692 个），而且重建走的是"直接按出生位置摆好"，于是**一帧之内平均高度从 727 跳到 801**：加载界面那批点消失、山上凭空出现另一批点，看不出任何过渡。现在三处一起兜住：
  - 尺寸变化小于 `REBUILD_PX`（48px）一律忽略，只把画布对齐一下，粒子一个都不动；
  - 真的变了窗口大小、并且发生在归位途中，就**把开场整个重播一遍**（点重新散开再归位），绝不跳到终点；
  - 开场计时改为从**第一帧**起算，首屏被字体或脚本卡住时也不会整段动画被跳过。

### 新增

- **加载条长成页眉**：开场那根加载条不再"滑走消失"，而是**整体上移、停进页眉的位置**，随后进度条向两端延伸成页眉底下那条通栏细线，百分比淡出、导航链接淡入 —— 从此它就是页眉本身。
  - 于是 `components/Preloader.jsx` 与 `components/Nav.jsx` 合并成 `components/Header.jsx`（+ `Header.css`）：一个元素演完全程。
  - 关键在于它**始终是 `position: sticky; top: 0`**（在文档流里占着页眉那 56px，页面内容不会少一块），加载期间只是用 `transform: translateY(calc(100dvh - 100% - var(--load-pad)))` 把自己挪到屏幕底部 —— 到位那一刻既不用换元素，也不会让页面跳一下。
  - 时间轴与粒子归位对齐：`INTRO_MS`(2000ms) 进度满 → 页眉上移 900ms（纸色遮罩同时上滑）→ 到位后进度条延伸 600ms、百分比与链接原地交叉淡变 → 粒子归位结束时页眉降回普通层级（z-index 9999 → 900）。
  - 加载时页眉的排版**和最终页眉完全一致**（同一个 `.header__inner`：56px 高、两边 `var(--pad)`），只有右边那一格从百分比换成链接。
  - 首屏文字的推入延时相应顺延到 2.9s 起（页眉落位之后）。
- **回到顶部**：右下角一颗圆形箭头（`components/BackToTop.jsx`），滚过 420px 才淡入，点一下平滑滚回顶部（开了减少动态效果就直接跳）。它只在可见时才进 Tab 序列，藏起来时对鼠标和键盘都是"不存在"的。
- **所有点统一大小、一样黑**：粒子直径不再随机（去掉 0.6~1.6 倍差异），透明度也从"随墨量变化"改为恒定 —— 山的明暗只剩一个变量：**疏密**。轮廓完全靠"哪里有格子被点亮"读出来。
- **开场无序云 → 屏幕底部凝聚成山**：加载界面显示期间，这一批点无序散在一条**和加载进度条等宽、半屏高、垂直居中**的带子里缓慢漂浮（画布临时 `position: fixed` 铺满屏幕，层被抬到遮罩背景层之上、文字层之下）。加载结束的那一刻开始归位，1.5 秒内落到底部凝聚成山的轮廓（错峰 420ms + 单点飞行 1080ms，`easeOutBack` 只留一点点过冲）。
- **点更小、更密**：格子边长 3~4px（`CELL_MIN/MAX` + `AREA_PER_CELL`），直径 = 格子 × 0.95，桌面端约 1.5 万颗点 —— 点与点之间留得出空隙，深色处又仍能压实。
- **抖动换成交错梯度噪声（IGN）**：取代原来的 8×8 Bayer 矩阵。颗粒感更接近蓝噪声 —— 是自然的沙粒而不是能看出周期的网点，浅色区域又比纯白噪声均匀、不结块。
- **山形重做**：四层山各有 `ridgeTone / baseTone / falloff / noise` 四个参数，山脊压实成实心剪影、往下迅速变稀，山谷里的雾带更干净，画面底部留出一小块亮地板。轮廓比之前清楚得多。
- **首屏整体落在一屏之内**：山景的可用高度改为跟着视口高走（`30vh`），归位时能看见整座山在屏幕底部凝聚，而不是被折线切掉。

### 变更

- **修：滚回首屏时左右箭头（和底部条）还飘在那里**。它们原来是"进过一次板块区（`armed`）就一直渲染"的 `fixed` 元素。现在多一个 `inView`（IntersectionObserver 盯着板块区），把箭头和底部条包进 `.tabs__nav`，板块区不在视野里就整体淡出（`opacity/visibility/pointer-events` 一起收）。实测：首屏未滚动不渲染、进了板块区显示、滚回首屏淡出、页脚（板块区还有约 76% 在视野里）照常显示。

- **关于我的信息区加"博客"字段**（`https://xiaji.xin`，排在邮箱后面）：`site.js` 的 `about.facts` 加一行；`About.jsx` 把渲染逻辑从"只认邮箱"改成通用 —— 值以 `http/https` 开头就渲染成新窗口打开的链接（显示时去掉协议头，所以看到的是 `xiaji.xin`）。顺带把邮箱那格不再单独通栏，于是"邮箱 | 博客"和"坐标 | 建站于"正好各占一行，四行两列对齐。

- **经历的图形重做**：换成**一座双峰点阵山**（呼应首屏那座山）+ **山顶一面旗** + 三条跟着山势走的**等高线** + 从山脚盘到峰顶的**之字形登顶脚印**（小方块，刻意不用线 —— 否则跟等高线糊在一起）+ 山脚线。

- **项目作品的图形再放大 + 卡片叠得更多**：叠卡从 3 张加到 **5 张**（后面四张各带标题条，错开露边，一眼是"一叠"）；这一章的图形缩放从 `data-fig-scale 0.86` 提到 **1**（填满留白穴，视觉上大了约 16%）。试过直接把这一章的穴放大 10%，但左边卡片栏一窄就翻成三行、反而把"一章一屏"顶破（1080 超出 17px、768 超出 102px），所以改成"穴不动、图形填满"这条路 —— 三档视口又回到零章内滚动。

- **图形的面积再放大一档、粒子更密**：`--slot-w` 全线 ×1.1（1920 下技能/作品/经历/联系 632、关于我 726、便签 360，含两档矮屏规则）；采样格子 **5px → 4px**（格子多了约 1.5 倍，点径跟着细一档），粒子总数 **6000 → 10000**。于是同一张图上的点更多、更精细，轮廓也更耐看。实测三档视口（1080/900/768）仍然**零章内滚动**。

- **五个图形推翻重画**（关于我的人像仍不动），每一个都按当页主体重新设计、比原来复杂一档：
  - **技能栈**：一叠横向条形（跟这一页那五行技能条同一种语法）—— 纵轴 + 底轴 + 五条"名称块 + 条 + 已填段" + 折线趋势 + 节点。
  - **项目作品**：三张**叠起来的项目卡片**（跟这一页三张卡对应）—— 后面两张露边，最前面那张有标题栏 / 缩略图 / 两行说明 / 两个标签 / 页脚。
  - **经历**：一条**时间轴**（跟这一页四条同一种语法）—— 主轴 + 四道刻度 + 四个越往后越大的节点 + 上行折线 + 尽头一面旗 + 底部年份位。
  - **联系方式**：一个**对话气泡**（带小尾巴、里面两行话）+ 从气泡飞出去的**纸飞机**与虚线航迹。
  - **留言便签**：一面**便签墙**（三张叠着、最上面那张带折角/标题/项目符四行）+ 右上角斜放的铅笔。

- **五个图形再加细节（关于我的人像按你说的不动）**：阶梯加**扶手斜线**；浏览器窗口加**滚动条 + 底部状态栏 + 第三条内容线**；经历加**路面中线虚线 + 两粒石子**；信封加**寄件人一行 + 邮票齿孔**；便签加**顶部回形针 + 折角内侧阴影线**。
- **每一章一套自己的入场**（原来六章共用一套）：项目作品卡片 `popIn`（从下推上来并轻微放大，动的是 `.grid-cell` 外壳，卡片自己的悬停倾斜不受影响）；联系方式 `wipeRight`（横向擦出，错落更大）；经历 `riseIn`（时间轴从下往上长）；留言便签 `unfold`（表单向"展开"）+ 便签墙 `riseIn`；技能栈保持"逐格从左往右"；关于我保持整块淡入上浮。幅度也一起加大了（26~34px、0.55~0.7s）。

- **首屏两颗按钮改了去向**：第一颗改成 **About Me → 关于我（`#about`，实心）**，第二颗是 **See My Work → 项目作品（`#work`，描边）**；原来的 **Write to Me（`mailto:`）按钮撤掉**。两颗都走统一落点，实测都是"板块顶 101 / 状态栏线差 0"。页眉右上角那颗 mail 图标和联系页里的邮箱没动。

- **入场动作幅度加大**：整块淡入 14 → **26px**（0.45 → 0.55s）；逐格入场 14 → **30px**（0.42 → 0.5s），格与格的间隔 70 → **90ms**；卡片横向擦出 0.5 → **0.62s**。实测技能四格的 `translateX` 时间线：−20/−30/−30/−30 → −4/−10/−26/−30 → −1/−3/−7/−21 → 归零。

- **章节里再加一层"逐格入场"**（原来只有整块错落淡入）：技能行按"序号 → 名称 → 条 → 数值"**从左往右依次就位**（每格 70ms，行与行之间再叠 `--d`，实测第 1 行 0.25 → 0.75/0.36 → 0.94/0.86/0.66/0.17，第 2 行慢半拍）；经历条目同样按"标记 → 年份 → 正文"从左滑入；项目/联系卡片**横向擦出**（`clip-path`，刻意不动 `transform` —— 卡片自己有悬停倾斜，动 transform 会把它顶掉）。都挂在 `.tabs.is-armed` 下，切章逐格重放，「减少动态效果」时被 base.css 统一关掉。

- **关于我的人像回到上一版**：去掉上一版加的"证件照外框 + 脖颈"，恢复成头 + 肩 + 地平线（`lib/figures.js` 的 `about()`）。尺寸仍是 +15% 那一版（穴宽 680）。

- **六章图形改为左右交替（3 左 3 右），并加上鼠标驱散**：
  - 位置：关于我·右 · 技能栈·**左（原右上，镜到左上）** · 项目作品·**右（原左上，镜到右上）** · 经历·左 · 联系方式·右 · 留言便签·**左（图形与表单左右互换）**。实测六章的穴中心相对板块中线：右 / 左 / 右 / 左 / 右 / 左，各 3 个。
  - **鼠标驱散**（`lib/figureParticles.js` 的 `MOUSE` + `step` 的 `mouse` 参数）：和主页那座山同一套一阶滞后 —— 坑内快速推开（带一点切向旋转），坑外缓慢流回，永不过冲。参数按"图形比山小"缩了一档：**半径 30 / 深 20**（山是 38 / 26），每颗粒子还带各自的 `weight`(0.75~1.3) / `curl`(±1) 让坑的边缘不齐。
  - 画布本身仍是 `pointer-events: none`，光标监听挂在 window 上、每帧换算成画布坐标；**任何一次 mousemove 都唤醒主循环**（包括光标离开时，好把点慢慢放回去），循环在"形变结束 + 按坑静下来"时自己停。
  - 实测：悬停时图形区域有 1385 个像素发生变化（坑按出来了），光标移开 3 秒后回到 519（点基本流回原位）；三档视口 1080/900/768 仍然全部"一章一屏"。

- **内容块的底色全部改成透明**：原来 `.pane > *`、章节标题、卡片（`.card`）、渠道卡（`.channel`）、时间线条目（`.tl__item`）、表单（`.notes__form`）、便签（`.note`）都垫了一层纸色（`var(--paper)`）用来挡粒子，现在全部 `transparent` —— 切板块时粒子从元素**背面**穿过去看得见（技能条数值、卡片边框、文字都浮在粒子之上，互相透出来）。卡片 hover 的底色从 `--paper-2` 换成 `rgba(17,17,17,0.022)` 的一层极淡墨色，保留反馈又不挡粒子。`.tabs` 自身那层纸色面保留（那是版面底色，不是内容元素）。

- **六个图形都加大 15%、各加 2~3 处细节**：
  - 尺寸：留白穴从 500 / 600 / 340 提到 **575 / 690 / 372**（`--slot-w` 全线 ×1.15，含两档矮屏紧凑规则），实测穴宽 572 / 680 / 356。便签那章为了守住"一章一屏"只放到 372（×1.09）。
  - 细节（`lib/figures.js`）：人像加**证件照外框 + 脖颈**；阶梯加**每级踏面边线 + 顶上小旗**；浏览器窗口加**侧栏分隔与条目 + 内容图位 + 另一条内容线**；经历加**起点圆点 + 三处里程碑底座**；信封加**邮票 + 收件人两行**；便签加**标题线 + 项目符 + 第四行**。
- **修掉"跳转落点"的一串问题**（页眉/页脚/首屏按钮全查了一遍）：
  - 病根有两个：锚点是打在**面板**（`.tabs__view` 的 id）上的，比板块顶多一个内边距；而目标章节**是隐藏的**时浏览器跳不过去，只会回到页首。加上浏览器把这次跳转推迟到 React 更新之后，和我们自己的对齐互相抢时序。
  - 现在由 `Sections.jsx` **接管所有 `a[href^="#"]`**（章节锚点）：`preventDefault` → 自己 `pushState` 写 hash → 切面板 → **自己算 `scrollTop` 对齐**（不再依赖 `scrollIntoView`，也不靠 `scroll-margin`）。箭头/键盘走另一条更宽松的"带回视野"，已经在视野里就不动，免得把用户正在看的位置拽走。
  - 实测 13 个入口（页眉 6、页脚 3、首屏 See My Work、箭头从页脚、回到顶部、页眉品牌 #top）**全部落在板块顶 101px / 状态栏线差 0**。

- **留言便签按参考图 3 的版面重排**（把图里那几个红框的坐标提出来对着改的）：
  - **引导句通栏** → **表单独占左边那一行**（实测 1920 下 173~1380）→ **粒子图形挪到同一行的右侧**（1423~1747，原来是"左下"，现在读作右中）→ **便签墙在下面通铺整宽**（173~1747，比表单宽 367px）。
  - 好处顺带有了：墙从"右半边 1016px"变成整块宽，一行能多放两张便签；这一章的高度也从 830 降到 826 以内，1080 / 900 / 768 三档都更宽裕。
  - 六章现在的位置：关于我·中右（证件页）· 技能栈·右上 · 项目作品·左上 · 经历·左中 · 联系方式·右下 · 留言便签·右中。

- **留言便签的图形加上一支铅笔**（照参考图 3 的手绘）：便签纸右上角斜放一支铅笔（笔杆 + 金属箍 + 笔尖 + 笔尾），`lib/figures.js` 的 `notes()`。
- **试过 Word 那种"文字环绕"，评估后没有采用**（记一笔免得以后重复踩）：
  - 做法本身可行：给留白穴加 `float` + `shape-outside: circle(closest-side)`，正文就会绕着图形排。
  - 但在这套版式里代价太大：① 正文压着 40em（约 46 字/行）时**根本碰不到**图形，要环绕就得把行放到 900px 以上；② 正文块是不透明纸色底（`.pane > *`，用来挡粒子，不然切章时点会糊在字上），一环绕就**把图形切成几截**；③ 表格/卡片这类块状内容是 BFC root，**只能矩形避让**、绕不了。
  - 所以：关于我保持"证件页"分区（照图 1，照片在右、字段在左）；经历保持左中留白穴；六章的图形仍是"嵌在留白穴里 + 矩形避让"，切章时粒子从内容块底下穿过去，已经有环绕的观感。

- **"一章一屏"做成硬约束，并让首屏状态栏那条线跟页眉线重合**：
  - 新增两个基准量：`--header-h: 56px`（页眉）与 `--status-h`（首屏底下那条状态栏的高度，由 `App.jsx` 量出来写回，随字号/换行自适应）。
  - **滚动落点**：`html { scroll-padding-top: calc(var(--header-h) + var(--status-h)) }`（1080p 上是 101px）—— 于是跳到任意章节时，**首屏状态栏那条线正好落在页眉线上（两线重合）**，"真理会过时，构石永恒 / 向下滚动 · SCROLL"紧贴页眉下方。实测箭头、页眉链接(hash)、键盘三种入口落点完全一致（状态栏线 56 = 页眉线 56，板块顶 101）。
  - **首屏**改成 `min-height: calc(100vh - var(--header-h))`（原来 100vh，底部状态栏会被折线切掉 56px）；**每一章**改成 `calc(100vh - var(--header-h) - var(--status-h))` —— 一章正好填满"状态栏以下那一屏"。
  - **不许章内滚动**：穴、间距、标题、行高全部改成跟视口高挂钩（`vh` 段），并加了两档矮屏模式（`max-height: 980px` / `820px`）逐级收紧。实测 1920×1080 / 1440×900 / 1366×768 三种尺寸、六章全部"面板高 ≤ 可用高"，**零溢出**（改之前 1440×900 上经历那章要滚 158px）。

- **导航整个换掉：左侧那列文字选项取消，改成"屏幕两侧箭头 + 底部一条线"**（照参考图）：
  - **左侧选项卡栏删掉**（`.tabs__rail` / `.tab` / 滑动竖线那套 CSS 与 JS 一并清掉），板块变成**单栏**：宽度 `clamp(620px, 82vw, 1680px)`（把左栏腾出来的空间吃掉了），仍然在屏幕上居中（1920 上左右各 173）。
  - **左右箭头**：细 chevron，`position: fixed` 贴在屏幕左右边缘、竖直居中，到头循环；鼠标悬停会往对应方向轻移 3px。**它们在 section 外面渲染** —— `.tabs` 上有 `clip-path`，fixed 子元素会被裁掉。
  - **底部条**：固定在视口底部的一条 3px 通栏细线 + 一段"当前位置"（宽度 1/6，`translateX(i × 100%)` 滑过去，`0.5s` 缓动），**里面不写字**。箭头和底部条都在"整块第一次滚进视野"之后才出现（`armed`），免得跟开场那根加载条打架。
  - **键盘**：←/→ 切相邻章节（在 input / textarea 里时让开，不抢光标键）；页眉那几个锚点链接照旧。
  - **面板里加回章节标题**（之前交给左栏、视觉上藏起来了）：中文大字 `clamp(26px, 2.7vw, 42px)` + 下面一行小号英文（大写字距）+ 一根通栏细线，序号不再显示（位置由底部那条线表示）。
- **关于我按参考图改成"身份证信息页"**：字段值下面加了一条**填空线**（`dd` 的 `border-bottom`），并补了两格 **坐标 / 建站于**（`site.js` 的 `about.facts`，现在是 7 条）；"通栏那一格"改成按标签判类名（`.is-wide`），不再依赖 `:last-child`（否则最后那格会变成"建站于"）。邮箱本身是链接，不再叠一条线。

- **关于我改成"身份证信息页"那种语法**：
  - 自述（引导句 + 四段）留在上方当引言；下面那五个字段（昵称 / 身份 / 座右铭 / 最近在做 / 邮箱）改成 **标签小灰字在上、值在下**，**两列排开、最后一格（邮箱）通栏**，并去掉原来的细线分隔（`About.css`，`.about__fact:last-child { grid-column: 1 / -1 }`）。
  - 值的字号从 14px 提到 `clamp(15px, 1.2vw, 17px)`，标签仍是 11px mono 大写字距 —— 一眼是"信息页"而不是"表格"。
  - 右边的粒子图形（半身像）就当那张**证件照**，位置和 6:4 的比例不变，外面不加任何框。
  - 手机上（≤720px）字段区收成单列。

- **按 1080P 的观感调一轮：整体居中 + 各章节的图形位置与排版**：
  - **整组在屏幕上居中**：`.tabs__inner` 的 `max-width` 现在写成 `左栏 + 间距 + 板块 + 两侧留白`，再 `margin: 0 auto` —— 1920 上左右留白各 **94px**（之前左边 56、右边 132，整体偏左）。
  - **关于我**：信息表（昵称 / 身份 / 座右铭 / 最近在做 / 邮箱）从"右侧一栏"挪到正文**下面**，图形留在右边并放大到 **600px** —— 左边"正文 + 信息表"与右边图形约 **6 : 4**。
  - **技能栈**：删掉 HTML & CSS、JavaScript 两条（`data/site.js`，剩 5 条）；行距从 16px 拉到 `clamp(22px, 2.5vw, 34px)`（5 行摊掉原来 7 行的分量），整表再往下挪一点；阶梯图形从右中挪到**右上角**。
  - **项目作品**：图形挪到**左上角**，卡片整体下移一点（`margin-top` 一档）。
  - **联系方式**：图形从左上改到**右下**（左上让给项目作品了，否则两章撞在同一个位置）。六个位置现在是：关于我·中右 / 技能栈·右上 / 项目作品·左上 / 经历·左中 / 联系方式·右下 / 便签·左下。
  - **经历换图形**：原来的"时间轴 + 三个菱形"换成**一条蜿蜒向前的路 + 三面里程碑小旗 + 尽头的箭头**（`lib/figures.js` 的 `journey()`）。
  - **图形的落点偏移归零**：`PLACEMENT` 的 dx/dy 全部取消、统一 0.94 内缩 —— 位置差异现在完全由各章节的留白穴负责，图形老老实实居中待在穴里（大小差异仍由 `data-fig-scale` 给）。

- **粒子图形从"右边的背景层"改成"嵌进章节里的留白穴"**（这一版改动最大的一处）：
  - **每个章节留一块空白，位置各不相同**：关于我·右上 / 技能栈·右中 / 项目作品·右下 / 经历·左中 / 联系方式·左上 / 留言便签·左下。六处两两不重叠，所以切板块时图形是大幅度平移（还带一点缩放），"从一个章节嵌进另一个章节"。
  - **板块加宽到 `min(78vw, 1560px)`**（1920 下 1498，原来是 998），每个章节按"内容 + 穴"重新排：关于我 = 正文 | 信息表 | 穴（**≤1699px 换两栏**：正文 | 穴、信息表落到正文下面 —— 三栏在这个宽度会把正文挤成 20 来个字一行）；技能行 1060；作品卡片 3 列；经历时间线 1016；渠道卡 1498 通铺；便签表单上限 1100、便签墙落在右下。
  - **画布从"贴着右边一角的正方形"改成铺满整块板块区**（1498×1000 量级），图形的位置和大小由"当前章节那个穴"量出来（`FigureCanvas` 的 `slotBox()`）—— 粒子从一个穴飞到另一个穴。每个穴还带 `data-fig-scale`（0.82~1.0），所以除了平移还有轻微缩放。
  - **内容块自己垫纸色**（`.pane > *:not(.pane__slot)`，面板本身不再有底色）：粒子从底下载过去时不会糊在字上 —— 截图里能看到点从信息表后面穿过去、再从另一侧出来。
  - **画布大了，就不能每帧全清**：改成只清"上一帧的点 + 这一帧的点"的并集（脏矩形）。实测一次切换的 1.5 秒里跑了 78 帧（headless 软件光栅，约 52fps）。
  - **≤1279px 收穴、章节摊回一列**：放不下就不留空白（穴 `display: none`），图形照旧藏起来；手机上同样是一列铺满。
  - 便签那一章内容最多，穴单独收一档（`clamp(260px, 22vw, 400px)`）才塞得进一屏；六章高度 560~787，都在一屏内。

- **内容板块整理 + 过场动画统一**：
  - **头部语法统一**：联系 / 便签原本是 26~54px 的大标题 + 副标题，其余四块是一句小引导句；现在六块都用同一条 `.sec-lead`（那两句大标题并进引导句里），`base.css` 里的 `.section-headline` / `.section-sub` 随之删掉。
  - **进场元素统一**：技能那 7 行、经历那 4 条以前不在 Reveal 里（切过去是硬切出现），现在六块统一成"标题 → 引导句 → 条目"逐条入场，间隔 55~65ms（技能行 70/125/180…400ms、作品与联系卡片 70/130/190ms、关于我段落 70/130/190 + 信息表 250ms）。
  - **切板块时整块重放入场**：面板里的元素不再走"滚到自己才淡入"（0.85s / 26px、一块一块来），改成"这块面板正在显示"就演一遍 —— 0.45s / 14px + `--d` 错落，跟你要的"快而克制"。整块第一次滚进视野之前不放行（`.tabs.is-armed` 由 IntersectionObserver 给），否则动画会在页面顶部就演完、滚下来什么都看不到。
  - **选中竖线改成一根滑动的线**：原来每个选项各带一条 `::before`，切换是"这条灭、那条亮"；现在整栏只有一根 `.tabs__ind`，位置和尺寸由 JS 量出来写进 `--ind-*`，切换时 0.42s 滑过去（实测 top 390 → 458）。窄屏那排横标签用同一套变量，自动变成底下那条线（实测与选中项的 left/right/bottom 完全对齐）。
  - **幽灵字交叉淡入**：左下角那个 ABOUT / SKILLS 以前是硬切，现在旧的往右淡出、新的从左边淡入，两个名字叠在同一格（`grid-area: 1/1`）里交接 —— 实测过渡中两个 span 并存，旧 0.2→0、新 0.6→0.9→1。
  - **内容先到、粒子后到**：换板块时图形延后 `SWITCH_DELAY_MS`(160ms) 才开始变形（实测正文 0~70ms 起步、图形第 3 帧 ≈160ms 才开始变），层次更分明；连点两下时前一次会被取消，只有最后选中的图形会动。
  - 开了「减少动态效果」时全部内容直接可见、不延后（实测六块元素 opacity 全 1、幽灵字 1、图形照常画出来）。
  - 顺带修回：技能行的 hover 底色过渡（`.reveal` 的 transition 会盖掉它，现在面板里换成"只留组件自己的过渡"）。

- **选项卡区改成三栏，右侧多了一块"粒子图形"**：左（选项卡，收窄到 140–186px）· 中（主内容宽 `clamp(620px, 52vw, 1000px)`，在屏幕上居中）· 右（粒子图形）。每个板块有自己的一张几何图形 —— **人像 / 阶梯 / 浏览器窗口 / 时间轴 / 信封 / 便签**，全部写在 `lib/figures.js` 里（`fillRect` / `arc` / 路径，画在 100×100 的归一化画布里），由 `FigureCanvas` 按 5px 格子采样成点阵。
  - **换板块时粒子直接"挪"过去，不再散开重聚**：每颗粒子配好目标格子后直接插值飞过去（单点 780ms + 错峰 260ms），中途看得出形状在变形，但不会先糊成一片再收拢。落定后主循环停下、不占帧；第一次滚进视野也会先聚一次。
  - **粒子总数固定 6000 颗**（格子多就抽稀、少就复制补齐 —— 复制点落在同一格子中心、像素上完全重合），所以切板块时不会有粒子凭空出现或消失。
  - 点阵**刻意不加抖动**：一抖就从"整齐的点阵"变成"噪点云"了（图形要的是参考图那种一格一颗的语法）。
  - **正文栏宽度跟着屏幕长**（`--col-w: clamp(620px, 52vw, 1000px)`）：1440 → 749、1680 → 874、1920 → 998、再宽封顶 1000。第一版固定 620 在 1920 上右边空着 488px，很别扭。
  - **正文往左贴住左栏**，跟选项卡看上去是一整块（试过两头：先在"左栏与图形之间"居中、再改成屏幕居中，最后发现贴住左栏的耦合感最好）。实现是负 `margin-inline-start`：这一栏从"左栏右缘 + 间距"开始，`justify-self: center` 又把它推到栏中间，所以要往左拉回"整栏的富余量"（`calc(-1 * var(--pull) * max(0px, 100% - 栏宽))`，100% 指栅格宽度）；取满富余量时，负 margin 把 margin box 撑大到整栏、居中再摊回去一半，位移恰好落到栏的起点。`--pull` 就是拉的比例：**1 = 贴住左栏（默认）**、0 = 在栅格里居中，想找中间值改一个数即可。实测 1920 上正文 290~1288（左栏右缘 242，间距正好是版面的 48px），任何宽度都不压左栏。
  - **一行别跟着变长**：栏可以到 1000，但纯文字段落压到 ~40em（约 40 个汉字），多出来的宽度交给"结构"——卡片列数、信息表并排。
  - **图形是一层背景，内容压在它上面**：`position: absolute`、贴右边缘、宽 `min(58vh, 720px)`，正文那一栏不透明。
  - **图形尺寸收回 0.75**：第一版 420px、上一版放到 842px（1920×1080），这一版因为改成背景层，收到 `min(58vh,720px)`（同屏 626px），不跟内容抢。
  - **≤1450px 干脆把图形整块藏起来**：正文放大后，右边再挤一块 522~626 的图形就只剩一条"残影"了（1366 上实测图形被盖住 31%），不如不露；这一档正文照样在屏幕上居中。
  - **图形整体往右挪了画布宽的 7%**（`right: calc(var(--fig-w) * -0.07)`）：画布右边本来就有 8~24% 的空白（最少的"技能 / 联系"是 8%），挪这么多不会切到任何一颗墨点 —— 六张图 × 1000 / 700 / 1200 三种视口高实测都没切，正文却少压一截、图形也更贴边。代价是它会伸出容器右边一截，而 `clip-path` 只管画、不管可滚动区域，整页因此多出一条横向滚动；所以 `.tabs` 上补了 `overflow: clip` —— 只裁不滚，不会像 `overflow: hidden` 那样把这里变成滚动容器、把手机端那排吸顶标签搞坏（390 实测标签仍吸在页眉下 56px 处）。
  - **正文往左挪之后，图形基本不再被压**（以墨点最靠左的"经历"那张为最坏情况）：1451 → 68px、1500 → 53px、1600 → 17px、1680 以上 → **0px**（1920 上正文右缘 1288、墨点左缘 1417，隔开 129px）。也就是说"贴左栏"和"图形完整"同时拿到了。
  - **每个图形的落点在画布里各不相同**（`figures.js` 的 `PLACEMENT`：scale 0.86~0.96、偏移 ±0.06）：换板块时粒子不是原地重排，而是"从这边挪到那边"，多了一层平移，过渡不至于生硬。
  - **栏宽了，面板内部跟着重排**（把宽度用在结构上，而不是把正文拉成一行 90 字）：关于我在 ≥1600px 变成"正文 + 信息表"两列并排；项目卡片 240px 起、联系方式 250px 起，栏宽到 1000 时能排到四列（项目只有三张，`auto-fit` 会把空轨道收掉，仍是三列铺满）；便签表单侧栏 200 → 240px。顺带修掉面板里那层 `.wrap` 的 `--pad` 内边距（它又把 883 的栏宽吃成 755，三张卡片因此只能排两张）。
  - 窄屏（≤860px）三栏挤不下，右侧图形整块隐藏、主内容占满整宽。
- **六个板块改成左侧选项卡**（参考图 3 那种语法）：左栏竖排六项 —— 中文大字 + 右侧小号英文 + 每条底下一根细线，选中项中文加粗变黑、左侧一条竖线；右栏只显示当前那一块面板，左下角压着当前板块英文名的大号幽灵字。
  - 六个面板**全部挂载**、只有当前的可见（`hidden`），所以切回来时留言便签的草稿、技能条动画都还在；章节标题在面板里视觉隐藏（读屏仍读得到），避免和左栏重复。
  - 地址栏与选项联动：`#about` / `#skills` … 刷新或分享都能直达；**页眉那几个链接点一下就是切面板**（它们本来就是 `#锚点`，所以页眉一行代码没改），并且会把选项卡区带回视野。键盘 ↑↓←→ / Home / End 也能切（标准 tablist 操作）。
  - 窄屏（≤860px）左栏收成顶部一条可横滑的标签行，并吸在页眉下面。
  - 顺便：区块级的「每节一屏」不再需要了（面板由选项卡那一屏来管），`base.css` 的 `.section` 因此瘦成一行；新增 `components/Sections.jsx` + `Sections.css`，`App.jsx` 里六个板块换成 `<Sections />`。
- **仓库转为私有，站点托管在 Cloudflare Workers**：线上地址 `https://xiaji-home.variant305.workers.dev`（免费版支持私有仓库；构建在 Cloudflare 侧跑，推 `main` 自动发布）。GitHub Pages 那条工作流已停用，旧地址随之退役。
- **托管准备迁到 Cloudflare Pages**（仓库要转私有）：`.github/workflows/deploy-pages.yml` 改名为 `.yml.disabled` —— 公开仓库转私有时 GitHub 会**自动下线**已发布的 Pages 站点（Free 账号），那条工作流随后每次推都会报错。README 的部署章节改成以 Cloudflare（Workers 静态资源，免费版支持私有仓库、构建在 Cloudflare 侧跑、不需要任何工作流）为主线，Cloudflare Pages / Vercel / Netlify 作为备选，GitHub Pages 退为"公开仓库 / Pro 以上"的备选。
- **滚动散开改成"逐层错开"**：原来是从画面中线往两边裂开（中线左边的往左飞、右边往右飞），现在改成按粒子**所属的山层**反向飞 —— 最前面那层往左、往后依次右 / 左 / 右，同一层里再按各自的 `speed` 有快有慢。于是下滑时整座山像几张纸片错开滑走，而不是从中间裂开。为此 `computeShade()` 现在一并返回 `layer`（每个格子属于哪一层山），`buildParticles()` 拿它给每颗粒子定下 `dir`。（顺带一提：这四层的点数是极不均的 —— 最前那层占 80.8%，所以视觉上主要是"前山往左、远山往右"。）
- **章节统一高度**：六个正文章节（关于我 / 技能 / 项目 / 经历 / 联系 / 便签）现在**每节都至少占满一屏、内容垂直居中** —— 改之前是 0.67~1.10 屏不等，节奏不齐。桌面端 1440×900 与 1920×1080 实测六节都是整一屏、上下留白相等；窄屏上内容多的那几节（技能 / 项目 / 便签）会自然变高，不裁切也不压缩。上下留白同时从 `clamp(64px,9vw,132px)` 收到 `clamp(48px,5.5vw,88px)`（多余的空间交给居中分配）。首屏与页脚不参与统一。
- **鼠标驱散重做**：从"半径 120px、最深 58px 的大软坑 + 弹簧回弹"改成**光标底下按出一个小坑**——半径 38px、最深 26px、跟随更快；回位不再用弹簧，而是朝原位做**指数收敛（一阶滞后）**：点是"被推走后停在原地、再缓缓流回"，全程不过冲、不余振。`SPRING` / `DAMPING` 两个参数随之取消，换成 `MOUSE_FOLLOW` / `RETURN_RATE`。
- 开场遮罩时长 1.5s → **2s**，首屏文字的推入延时同步顺延；归位仍是 1.5s
- 绘制链路简化：静止时所有点都在同一个透明度桶里 —— 整座山就是**一次 fill**
- 开场遮罩拆成两层（`Preloader.jsx` 的 `.pre-bg` 背景层 z-index 9989 + `.pre` 内容层 9999），粒子云夹在中间；层级约定写在 `Hero.css` 的 `.hero--intro`，铺满视口写在 `ParticleMountain.css` 的 `.mountain--full`
- 鼠标视差的位移改由 CSS 变量承载、真正的 `transform` 写在 `.mountain` 自己身上（父层一旦有 transform，铺满视口的 `fixed` 画布就会失去参照）
- `lib/dither.js` 的 `renderMountain()` 改为直接画**圆形点阵**（6 倍格子分辨率），降级图与粒子版长得一样；不再用 `image-rendering: pixelated` 放大方块
- 减少动态效果的样式补上 `animation-delay: 0s` —— 否则首屏文字要干等两秒才出现
- `components/DotMountain.jsx` 由 `components/ParticleMountain.jsx` 取代
- `lib/dither.js` 拆分为 `computeShade()` / `isLit()` / `renderMountain()`，墨量数据由位图与粒子共用

### 计划中

- 便签墙支持排序与搜索
- 深色模式切换（可选）
- 提供英文版文案

## [1.0.0] - 2026-01-05

### 新增

- 首屏：两行大标题的逐行推入动画、副标题、两颗胶囊按钮
- **点阵山景**：由分形噪声生成四层山脊，再用 8×8 Bayer 有序抖动渲染成方块颗粒，全页零图片素材（`src/lib/dither.js`）
- 鼠标移动时山景与标题的视差位移（仅在精确指针设备上启用）
- 自定义圆圈光标：跟手小黑点 + 带惯性拖尾的圆环，悬停可交互元素时放大
- 开场遮罩：`000 → 100%` 进度 + 2×2 方块标志动画
- 滚动揭示：进入视口时淡入上浮（`IntersectionObserver`，不引第三方动画库）
- 关于我、技能栈（进度条生长 + 数字滚动）、项目作品（悬停 3D 倾斜）、经历时间线、联系方式、留言便签、页脚表格网格
- 留言便签：100 字上限、存 localStorage、可单条撕掉
- 页脚斜线阴影条缓慢流动
- 自适应布局：桌面 / 平板 / 手机
- 无障碍与体验：完整响应 `prefers-reduced-motion`，自定义光标仅在鼠标设备启用，键盘可见焦点，语义化标签与 `aria-label`

### 工程化

- React 19 + Vite 8 工程；文案集中在 `src/data/site.js`
- ESLint 10（扁平配置）+ Prettier，含 `npm run check` 一键全检
- GitHub Actions：CI（lint + 格式 + 构建）与 GitHub Pages 自动部署
- Issue / PR 模板、Dependabot、EditorConfig、Git 属性、VS Code 推荐配置

[Unreleased]: https://github.com/Xiaji-yu/xiaji-home/compare/v1.0.0...HEAD
[1.0.0]: https://github.com/Xiaji-yu/xiaji-home/releases/tag/v1.0.0
